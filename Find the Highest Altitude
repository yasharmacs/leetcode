class Solution {
    public int largestAltitude(int[] gain) {
        int a=0;
        int b=0;
        for(int i:gain)
        {
            a=a+i;
            b=Math.max(b,a);
        }
        return b;
    }
}
