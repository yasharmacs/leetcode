class Solution {
    public String defangIPaddr(String address) {
        String ram="";
        for(int i=0;i<address.length();i++)
        {
            if(address.charAt(i)=='.')
            {
                ram=address.replace(".","[.]");
            }
        }
        return ram;
    }
}
